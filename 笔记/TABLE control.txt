
1、TABLE control 控件刷新行数据
DESCRIBE TABLE GT_SMPD LINES CONSMPD-LINES.


2、列表操作后，数据更新到内表
 PBO:
 LOOP AT GT_SMPD  WITH CONTROL CONSMPD.
 ENDLOOP.
 
 PAI:
  LOOP AT GT_SMPD.
    MODULE UPDATA2TAB. "更新内容为：MODIFY GT_SMPD  INDEX CONSMPD-CURRENT_LINE.
  ENDLOOP.
  
  
3、获取选中的行数据。
  DATA:LINEA TYPE I.
  GET CURSOR LINE LINEA.
  CHECK SY-SUBRC = 0.
  LINEA = CONDH5-TOP_LINE + LINEA - 1.
  READ TABLE gt_XXX into LS_XXX INDEX LINEA. 